{% trans_default_domain 'app' %}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'todo creation' | trans }}</h1>

    {{ include ('@App/todos/_new_form.html.twig') }}

    <a href="{{ path('todos_index') }}">{{ 'back to the list' | trans }}</a>
{% endblock %}
