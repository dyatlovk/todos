{% extends 'base.html.twig' %}

{% block body %}
    <h1>Todo edit</h1>

    {{ form_start(edit_form, { 'attr': {'class': 'uk-form'} }) }}
        {{ form_widget(edit_form) }}
        <input type="submit" class="uk-button uk-button-primary" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('todos_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
